{
  "name": "po-module-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "validate:oracle": "ts-node-dev --transpile-only --no-notify --exit-child scripts/validate-oracle.ts"
  },
  "dependencies": {
    "express": "^4.21.0",
    "oracledb": "^6.6.0",
    "zod": "^3.23.0",
    "dotenv": "^16.4.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/cors": "^2.8.17",
    "@types/node": "^22.0.0",
    "typescript": "~5.5.0",
    "ts-node-dev": "^2.0.0"
  }
}
